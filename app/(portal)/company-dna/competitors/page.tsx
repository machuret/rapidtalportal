import { redirect } from "next/navigation";

/** Competitor setup and analysis now share one continuous workspace. */
export default function LegacyCompanyCompetitorsRoute() {
  redirect("/competitors");
}
